# Get list of rules from a party model.

This is a copy of `partykit:::.list.rules.party()` that is exported for
use in the causal distillation tree framework.

## Usage

``` r
.list.rules.party(x, i = NULL, ...)
```
