# Get depth of each node in a party model.

Get depth of each node in a party model.

## Usage

``` r
get_party_node_depths(party_fit, return_features = FALSE)
```

## Arguments

- party_fit:

  A `party` object.

- return_features:

  Logical indicating whether to return the feature associated with each
  node.
